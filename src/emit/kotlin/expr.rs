//! `Expr` → Kotlin source.
//!
//! Phase 1 skeleton — empty. Phase 2 ports the IR expression walker here,
//! the analog of `src/emit/typescript/expr.rs` (the largest emitter file).
//! Until then `kotlin::emit` produces only the Gradle scaffold, so nothing
//! references this module yet.
