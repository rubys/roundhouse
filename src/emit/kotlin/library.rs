//! `LibraryClass` → Kotlin file.
//!
//! Phase 1 skeleton — empty. Phase 2 ports the kind-agnostic class/module
//! walker here (the analog of `src/emit/typescript/library.rs`'s
//! `emit_class_file` / `emit_function_file`), driving the model →
//! controller → view emit against the lowered IR.
