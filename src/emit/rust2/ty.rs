//! `rust2` type rendering — `Ty` → Rust source-text.
//!
//! Phase 1 stub. Will receive a near-verbatim port of the existing
//! `src/emit/rust/ty.rs::rust_ty` (already-generic per the Phase 0
//! audit) once Phase 2 needs it. Kept as a separate file so the
//! eventual port slots in cleanly without disturbing the rest of
//! the rust2 module tree.
