# Built by ../build.sh (local) or CI (build-wasm job) — not committed.
# Rebuilt from source via the in-repo vendored ruby-rbs-sys; see ../vendor/.
roundhouse_wasm.wasm

# Playground app-picker artifacts: the manifest + the lobsters bundle are
# generated by CI (from a pinned ruby-bench SHA; see the build-site job).
# Absent the manifest, /playground/ falls back to the committed blog fixture.
apps.json
app-lobsters.json
app-mastodon.json
